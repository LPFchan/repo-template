# repo-template sync manifest
#
# One entry per line: <scaffold-relative source> -> <repo-relative destination>
# Lines starting with # are comments. Files and directories listed here are
# TEMPLATE-MANAGED: scripts/sync-template.sh copies them verbatim into
# adopted repos on every sync. Directories sync recursively; files that exist
# in a repo's managed directory but not in the scaffold are deleted.
#
# Everything NOT listed here is repo-specific and is never touched by sync:
# records/SPEC.md, records/STATUS.md, records/PLANS.md, records/INBOX.md,
# records/decisions/, records/research/, and any content in AGENTS.md after
# the <!-- template-managed:end --> marker.

AGENTS.md managed-section
CLAUDE.md -> CLAUDE.md
skills/ -> skills/
records/upstream-intake/ -> records/upstream-intake/
new-commit-message.sh -> scripts/new-commit-message.sh
check-commit-standards.sh -> scripts/check-commit-standards.sh
install-hooks.sh -> scripts/install-hooks.sh
sync-from-template.sh -> scripts/sync-from-template.sh
template-sync.yml -> .github/workflows/template-sync.yml
.githooks/ -> .githooks/
